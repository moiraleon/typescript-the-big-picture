{
    "compilerOptions": {
        "target": "es2015",
        "outDir": "./js",
        "strict": true,
        "noUnusedLocals": true,
     
    },
    "exclude": ["./js"]
}
//generating a file can be done by running tsc --init in the terminal
